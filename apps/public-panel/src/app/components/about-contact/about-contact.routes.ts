import { Routes } from '@angular/router';
import { AboutContactComponent } from './about-contact.component';

export const ABOUT_CONTACT_ROUTES: Routes = [
  {
    path: '',
    component: AboutContactComponent,
  },
];
